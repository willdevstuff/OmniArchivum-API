Current tech stack

.NET 10 (ASP.NET Core Web API)

PostgreSQL 16 (via Docker Compose)

Entity Framework Core + Npgsql provider

Built-in OpenAPI (AddOpenApi, MapOpenApi)

Scalar for interactive API docs (Scalar.AspNetCore)

Git + GitHub (secrets excluded via .gitignore)

Current architecture

Pattern: layered API (Controller → Service → Data)

API Layer

NotesController

CRUD-ish endpoints + search endpoint

TagsController

Create/list tags

Link tag ↔ note

Service Layer

INotesService (contract)

NotesService (implementation)

Pagination rules

Soft delete behavior

Tag creation/linking

Full-text search query

Maps Entities → DTOs

Data Layer

OmniArchivumDbContext

EF mappings

Soft delete global query filter

Full-text search tsvector generated column + GIN index

Entities:

Note (markdown body, timestamps, soft delete, search vector)

Tag (name, optional category)

NoteTag (many-to-many join)

API Contracts

DTOs: CreateNoteRequest, NoteResponse, CreateTagRequest, TagResponse